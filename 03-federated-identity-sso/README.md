# Federated Identity & SSO Implementation for AWS Cloud Access Governance

## Project Overview

This project demonstrates how centralized identity and single sign-on can be used to manage AWS cloud access in an enterprise environment.

The goal is to simulate a real-world access governance model where users authenticate through an identity provider and receive role-based access to AWS using permission sets, user groups, and least privilege principles.

## Project Status

In progress.
